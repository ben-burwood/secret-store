[project]
name = "secret-store-backend"
version = "0.1"
description = "Backend for Sercret Store"
readme = "../README.md"
requires-python = ">=3.13"
dependencies = [
    "robyn>=0.84.0",
    "sqlalchemy>=2.0.49",
]

[tool.ruff]
line-length = 150
